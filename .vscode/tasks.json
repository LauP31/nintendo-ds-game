{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "docker run --rm -v \"${workspaceFolder}:/work:z\" -w /work skylyrac/blocksds:slim-latest make",
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": ["$gcc"]
    }
  ]
}